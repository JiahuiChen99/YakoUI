import {Endpoint} from 'butterfly-dag';

class YakoNodeEndpoint extends Endpoint {
    draw(obj) {
        let point = super.draw(obj);

        return point;
    }
}

export default YakoNodeEndpoint;
